# Hoist

## Overview

This is the Python back end code for my gym tracking app. 
I use Flask, though the object-relational mappings are hand-written SQL for the most part.
Sqlite is used as the database, but it wouldn't be difficult to port the database code to Postgres or something else.